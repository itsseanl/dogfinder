     //if subbreeds exist, map through and push to array
      // if (entry[1].length > 0) {
      //   // entry[1].map(breed => {
      //   //   const subBreed = `${entry[0]} ${breed}`;
      //   //   breedList.push(subBreed);
      //   // });
      //   return;
      //   //else, map the first value
      // } else {
      // }